creational_pattern_definitions = {
    "Abstract Factory": "interface to create families of related or dependent objects without specify their concrete classes",
    "Builder": "separate the construction of a complex object from its representation",
    "Factory Method": "define an interface for creating an object and let subclasses decide which class to instantiate",
    "Prototype": "specify the kinds of objects to create using a prototypical instance",
    "Singleton": "ensure a class only has one instance"
}
structural_pattern_definitions = {
    "Adapter": "convert the interface of a class into another interface clients expect",
    "Bridge": "decouple an abstraction from its implementation so they can vary independently",
    "Composite": "lets clients treat individual objects and compositions of objects uniformly",
    "Decorator": "attach additional responsibilities to an object dynamically",
    "Facade": "provide a unified interface to a set of interfaces in a subsystem",
    "Flyweight": "use sharing to support large numbers of objects efficiently",
    "Proxy": "an object functioning as an interface to something else"
}
behavioral_pattern_definitions = {
    "Chain of Responsibility": "command pass through processing objects until one handle it",
    "Command": "encapsulate a request as an object to perform an action or trigger an event at a later time",
    "Interpreter": "define a representation for the grammar of a given language along with an interpreter that uses the representation to interpret sentences in the language",
    "Iterator": "provide a way to access the elements of an aggregate object sequentially without exposing its underlying representation",
    "Mediator": "define an object that encapsulates how a set of objects interact",
    "Memento": "expose the private internal state of an object so it can be restored later",
    "Observer": "the subject maintains a list of its observers and notifies them automatically when something changes by calling one of their methods",
    "State": "allow an object to alter its behavior when its internal state changes",
    "Strategy": "allow selecting an algorithm at runtime and which family of algorithms to use",
    "Template Method": "method in an abstract class that defines the skeleton of an operation",
    "Visitor": "represent an operation to be performed on the elements of an object structure"
}



